insideWhile -->
    (blank;class;fungsi;imports;importsAs;importsFrom;importsFromAs;input;isitotal;komentar;looptotal;raise;assign;conditional;with);
    (blank;class;fungsi;imports;importsAs;importsFrom;importsFromAs;input;isitotal;komentar;looptotal;raise;assign;conditional;with),(space;"\n"),inside.

loopWhile -->
    (blank;space);
    (space;blank), "while", (space;blank), (numbers;variable;bracket), (space;blank), ":".